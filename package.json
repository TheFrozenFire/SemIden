{
  "name": "SemIden",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "circuit:build": "mkdir build && circom --wasm --sym --r1cs --output build circuits/claim_proof.circom",
    "circuit:test": "mocha"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.3.2",
    "install": "^0.13.0",
    "npm": "^8.1.3",
    "vuetify": "^2.4.0"
  },
  "devDependencies": {
    "circom_tester": "^0.0.7",
    "eslint": "^5.8.0",
    "sass": "~1.32.0",
    "sass-loader": "^10.0.0",
    "truffle": "^5.0.11",
    "vue-cli-plugin-vuetify": "~2.4.3",
    "vuetify-loader": "^1.7.0"
  }
}
