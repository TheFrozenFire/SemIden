{
    "name": "SemIden",
    "version": "0.0.1",
    "private": true,
    "devDependencies": {
        "eslint": "^5.8.0",
        "truffle": "^5.0.11",
        "circom_tester": "^0.0.7"
    },
    "dependencies": {
        "libsemaphore": "^1.0.16"
    },
    "scripts": {
        "build-semaphore": "cd semaphore && npm i && npm run bootstrap && npm run build && ./circuits/scripts/download_snarks.sh && cd contracts && npm run compileSol",
        "circuit:test": "mocha"
    }
}
